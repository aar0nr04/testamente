const plans = [{ name: 'Free', price: '$0 MXN', features: ['Catálogo público', 'Resultados informativos', 'Historial para usuarios'] }, { name: 'Pro', price: 'Próximamente', features: ['Tests con licencia', 'Reportes extendidos', 'Herramientas profesionales'] }, { name: 'Premium', price: 'Próximamente', features: ['Todo Pro', 'IA con límites seguros', 'Soporte prioritario'] }];
export function PlansPage() { return <section className="stack"><div className="page-heading"><div><span className="eyebrow">Planes</span><h1>Crece a tu ritmo</h1><p>Los pagos están desactivados hasta contar con backend y webhook de entitlements.</p></div></div><div className="grid">{plans.map((plan) => <article className="card plan-card" key={plan.name}><h2>{plan.name}</h2><strong>{plan.price}</strong><ul>{plan.features.map((feature) => <li key={feature}>{feature}</li>)}</ul><button disabled={plan.name !== 'Free'}>{plan.name === 'Free' ? 'Activo' : 'No disponible aún'}</button></article>)}</div></section>; }
